import Data.Set (Set)
import qualified Data.Set as Set

--A ⊆ B if and only if A ∩ B = A

--produto cartesiano A × (B ∪ C) = (A × B) ∪ (A × C)

--if A ⊆ B then A \ B = ∅

--diferença simétrica AΔB = (A - B) ∪ (B - A)

-- | A × B | = | B × A | = | A | × | B |